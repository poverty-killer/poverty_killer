Claude Terminal packet
PRESERVE!!!!!
You are Claude Terminal operating under Supreme Board law.
This is a repo-truth confirmation + deduplication recommendation pass only.
You are not authorized to rewrite, delete, migrate, simplify, consolidate, or “clean up” anything yet.
You are only authorized to:
* inspect live repo truth 
* confirm or falsify the attached authority-map audit 
* identify duplication and ownership conflicts 
* produce a proof-backed recommendation ledger for later Board approval 
The attached audit is provisional, not accepted as repo truth yet. Treat it as a hypothesis set to verify, not as authority. 
Governing doctrine
* preserve-first is absolute 
* no deletion authority 
* no simplification authority 
* no downgrade authority 
* no fake certainty 
* no recommendations without evidence 
* no “probably canonical” language without live proof 
* shortest file does not win 
* cleaner rewrite does not win 
* richest valid live baseline is presumed superior until proven otherwise 
* one truth is the target, but only after proof-backed consolidation planning 
Goal
Produce a Board-grade answer to this question:
Which files, classes, and enum surfaces are truly authoritative, which are duplicated or transitional, and what exact items could later be deleted, shimmed, re-exported, or merged if the Board approves?
Hypotheses to verify from the provisional audit
Verify or reject each of these:
1. app/execution/orchestrator.py is the practical execution spine 
2. orchestrator.py improperly carries inline broker logic or an inline PaperBroker 
3. app/execution/paper_broker.py exists as a separate sovereign paper-broker candidate 
4. app/execution/broker_manager.py is a competing broker implementation or legacy duplicate 
5. app/execution/order_router.py may overlap with broker-boundary or live-execution ownership 
6. app/execution/live_broker.py may overlap with router or broker ownership 
7. enum ownership is fragmented across: 
o app/models/enums.py 
o app/utils/enums.py 
o app/constants.py 
8. overlapping taxonomy may exist for: 
o RegimeType 
o SleeveType 
o OrderStatus 
o InternalOrderStatus 
o ReplayMode 
o EventType 
o SourceType 
Required investigation method
Part A — import and reference truth
Search the repo and show exact evidence for:
* imports of PaperBroker 
* imports of broker_manager 
* imports of paper_broker 
* imports of live_broker 
* imports of OrderRouter 
And all imports/usages/definitions of:
* RegimeType 
* SleeveType 
* OrderStatus 
* InternalOrderStatus 
* ReplayMode 
* EventType 
* SourceType 
For each, report:
* defining file(s) 
* importing file(s) 
* whether usage is active-path, peripheral, test-only, dead-looking, or unresolved 
Part B — definition truth
Show exact file and line evidence for definitions of:
* MasterOrchestrator 
* PaperBroker 
* any broker-manager broker classes 
* any live-broker classes 
* OrderRouter 
Then state whether orchestrator.py contains broker logic inline, merely instantiates broker logic, or is cleanly separated.
Part C — runtime path truth
Trace the active runtime path through:
* main.py 
* app/main_loop.py 
* app/execution/orchestrator.py 
Then prove:
* what execution object is created 
* what router object is created 
* what broker object is created 
* whether paper_broker.py is actually constructed on the live path 
* whether broker_manager.py is actually constructed on the live path 
* whether live_broker.py is actually constructed on the live path 
Do not infer. Prove from imports, construction sites, and call path.
Part D — duplication and authority truth table
For each domain, return:
* declared owner 
* observed live owner 
* other overlapping implementations 
* conflict status: 
o confirmed duplicate 
o transitional duplicate 
o harmless overlap 
o no conflict 
o unresolved 
* exact proof 
Domains:
* execution spine 
* paper broker 
* broker boundary 
* live broker 
* core contract enums 
* hardened authority enums 
* overlapping semantic constants 
* order lifecycle taxonomy 
* sleeve taxonomy 
* regime taxonomy 
Part E — deletion / consolidation candidate ledger
This is the key output.
For every candidate file/class/symbol that appears duplicative, return a ledger row with:
* candidate item 
* type: 
o file 
o class 
o enum 
o constant 
o helper 
o shim 
o import surface 
* current role 
* overlapping authority with what 
* proof of duplication 
* live runtime usage: 
o yes 
o no 
o unknown 
* import usage count or usage summary 
* danger level if removed: 
o critical 
o high 
o medium 
o low 
* recommended action: 
o keep as canonical 
o keep as shim 
o keep temporarily with re-export only 
o freeze pending later migration 
o candidate for delete after proof 
o candidate for merge after proof 
* Board precondition required before action 
Hard anti-hallucination rule
If you cannot prove live/runtime irrelevance, you may not recommend deletion as a clean fact.
In that case you must say one of:
* KEEP_PENDING_PROOF 
* SHIM_PENDING_PROOF 
* DELETE_ONLY_AFTER_IMPORT_ZERO_AND_RUNTIME_ZERO_PROOF 
Output format
Return exactly these sections:
1. Confirmed findings 
2. Rejected findings 
3. Unresolved findings 
4. Final authority map 
5. Duplication ledger 
6. Candidate deletion list 
7. Candidate shim / re-export list 
8. Candidate merge / consolidation list 
9. Minimal safe future patch order 
Special law for recommendations
Your recommendations must be split into two classes:
Class A — safe now as architectural truth
Only for items with direct, strong proof.
Class B — requires Board approval after patch or migration
For items that are duplicated but still imported, referenced, or plausibly part of runtime fallback behavior.
Forbidden behavior
* do not rewrite files 
* do not propose giant migrations 
* do not collapse rich files just because they overlap 
* do not assume deadness from age or style 
* do not recommend deleting anything that still has unresolved runtime use 
* do not turn constants.py into the loser automatically 
* do not turn utils/enums.py into the winner automatically 
* do not assume paper_broker.py wins merely because architecture prefers it 
* do not assume broker_manager.py loses unless usage proof supports that 
Board objective
We want one truth and one predator bot, not a repo ridden with duplication, split ownership, fake authorities, or overlapping execution semantics.
But we will only get there by:
* proving the current truth 
* identifying duplication precisely 
* preserving the richest valid implementation 
* deleting only after proof 
PRESERVE!!!!!

Board-facing recommendation list to get from Terminal
This is the exact recommendation structure Terminal should return for your later approval.
1. Likely keep-as-canonical candidates
These are the strongest current candidates, but Terminal must prove them:
* app/execution/orchestrator.py — likely execution spine 
* app/risk/unified_risk.py — risk authority, already Board-accepted 
* app/risk/position_sizing.py — accepted 
* app/risk/kill_switch.py — accepted 
* app/models/enums.py — likely current practical core enum authority for live surfaces 
* app/execution/paper_broker.py — likely intended paper broker authority, but must be proven against actual runtime use 
2. Likely duplication / conflict candidates
These are not approved for deletion. They are the first suspects:
* inline PaperBroker or embedded broker logic inside app/execution/orchestrator.py 
* app/execution/broker_manager.py 
* overlapping live execution semantics between: 
o app/execution/order_router.py 
o app/execution/live_broker.py 
* overlapping enum/taxonomy surfaces across: 
o app/models/enums.py 
o app/utils/enums.py 
o app/constants.py 
3. Candidate delete list Terminal should evaluate
These are candidates only, not approvals:
* app/execution/broker_manager.py 
o only if Terminal proves: 
* no runtime construction on active path 
* no meaningful import consumers that cannot be shimmed 
* no richer logic that would be lost 
* embedded inline PaperBroker inside app/execution/orchestrator.py 
o not “delete now,” but likely extract/eliminate duplication candidate 
* duplicate enum declarations in app/constants.py 
o only overlapping semantic enums, not literal/static constants 
* duplicate enum declarations in either app/models/enums.py or app/utils/enums.py 
o only after a canonical owner is proven and a safe re-export/shim plan exists 
4. Candidate shim / re-export list
These are often safer than deletion first:
* app/execution/broker_manager.py ? shim to canonical broker layer 
* losing enum surfaces ? temporary re-export shells to canonical owner 
* overlapping constants/enums in app/constants.py ? re-export or alias during staged migration 
5. Candidate merge / consolidation list
Terminal should identify whether these should become single-owner domains:
* paper broker ownership 
* broker boundary ownership 
* live broker ownership 
* regime taxonomy 
* sleeve taxonomy 
* order lifecycle taxonomy 
6. Recommendations Terminal must separate clearly
Terminal must not hand you one mixed pile. It must separate:
* safe to declare canonical now 
* safe to demote to shim later 
* safe to delete only after import/runtime proof 
* not safe to touch yet 

